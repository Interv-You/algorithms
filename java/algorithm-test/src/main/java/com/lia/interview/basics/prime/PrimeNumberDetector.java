package com.lia.interview.basics.prime;
/*
 * Given a number, determine if it is prime.
 * A prime number is a natural number greater than 1 that is only divisible by 1 and itself.
 *
 * Rules:
 * - Numbers less than or equal to 1 are not prime numbers
 * - 2 is the smallest prime number
 * - Negative numbers cannot be prime
 *
 * Example:
 *   input: number = 2
 *   output should be true (2 is the smallest prime number)
 *
 *   input: number = 7
 *   output should be true (7 is only divisible by 1 and 7)
 *
 *   input: number = 4
 *   output should be false (4 is divisible by 1, 2, and 4)
 *
 *   input: number = 0
 *   output should be false (0 is not a prime number)
 *
 *   input: number = -3
 *   output should be false (negative numbers cannot be prime)
 *
 * Performance expectation: O(√n)
 */
public class PrimeNumberDetector {

    /**
     * This method aims to check if a number is a
     *
     * @param number
     * @return
     */
    public static boolean isPrime(int number) {
        // Implement the algorithm here
       return false;
    }
}
