package com.lia.interview.basics.swapnumbers;

import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.Setter;

/*
 * Given a SwapNumber object with two field 'x' and 'y', implement a method that swaps the value of 'x' and 'y' WITHOUT CREATE ANY INTERMEDIATE VARIABLE.
 *
 *
 * Example:
 *   initial state: x=3, y=9
 *   after swap execution: x=9, y=3
 */
@Getter
@Setter
@AllArgsConstructor
public class SwapNumbers {

    private int x;
    private int y;

    /**
     * This method aims to swap x and y values without using any other variables !
     */
    public void swap() {
        // Implement the algorithm here
    }
}
