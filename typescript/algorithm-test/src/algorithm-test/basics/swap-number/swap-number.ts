/*
 * Given a SwapNumber object with two field a and b, implement a method that swaps the value of 'a' and 'b' WITHOUT CREATE ANY INTERMEDIATE VARIABLE.
 *
 *
 * Example:
 *   initial state: a=3, b=9
 *   after swap execution: a=9, b=3
 */
export class SwapNumber {
    constructor(private x: number, private y: number) {}

    swap(): void {
        // Implement the algorithm here
    }

    getX() { return this.x; }
    getY() { return this.y; }
}